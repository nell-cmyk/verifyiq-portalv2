---
phase: 09-runner-documentation-and-regression-operations
plan: 01
plan_id: 09-01
status: planned
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOCS-04, DOCS-05]
user_setup: []
must_haves:
  truths:
    - "D-01/DOCS-04: README exposes a task-first runner quick start for full
      runs, single targets, headed debugging, and native Playwright flag
      forwarding."
    - "D-02/DOCS-04: README includes a compact table for `all`, `public`,
      `auth`, `applications`, `activity`, `audit-logs`, `users`, and `roles`
      with exact commands, auth requirements, and coverage scope."
    - "D-03/D-14/DOCS-04: README preserves the Phase 8 product constraints:
      Audit Logs export controls are covered but same-run portal activity
      evidence remains MUT-05 blocked; Roles create/delete are covered but role
      edit and reversible permission-toggle remain MUT-07 blocked."
    - "D-05 through D-08/DOCS-04: README documents storage-state-first recovery,
      exact auth precedence, `VERIFYIQ_FORCE_LOGIN=1` scope, and recovery
      commands without printing secret values or serialized storage state."
    - "D-09 through D-12/DOCS-04: README documents failure debug order:
      `test-results/triage-summary.md`, then native Playwright artifacts."
    - "D-13/D-15/D-16/DOCS-04: README documents same-run mutation and visible UI
      cleanup rules, including `AUTOMATION <area> <run-id> <record-label>`."
    - "DOCS-05: Planning docs reflect v1.1 runner scope, Playwright
      source-of-truth rules, automation-owned mutation safety, and Phase 8
      product blockers."
    - "Phase 9 must not modify runner scripts, Playwright specs, auth
      precedence, or mutation safety helpers unless documentation verification
      exposes an existing defect."
  artifacts:
    - path: "README.md"
      provides:
        "Operator runner runbook, auth recovery, triage order, artifacts, and
        cleanup rules"
      contains: "Portal Runner Operations"
    - path: ".planning/PROJECT.md"
      provides: "Project and milestone decision alignment"
      contains: "Phase 9 runner documentation"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 plan completion and milestone traceability"
      contains: "09-01"
    - path: ".planning/STATE.md"
      provides: "Current GSD state and next operator command"
      contains: "Phase 09"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DOCS-04 and DOCS-05 completion state"
      contains: "DOCS-04"
  key_links:
    - from: "README.md"
      to: "scripts/run-portal-automation.mjs"
      via: "runner target table and artifact paths"
      pattern: "VALID_TARGETS"
    - from: "README.md"
      to: "tests/support/auth-state.ts"
      via: "auth-state precedence and recovery"
      pattern: "VERIFYIQ_STORAGE_STATE_JSON"
    - from: "README.md"
      to: ".planning/phases/08-deep-portal-workflow-coverage/08-LIVE-INSPECTION.md"
      via: "MUT-05 and MUT-07 product constraints"
      pattern: "MUT-05 Audit Logs evidence blocker"
---

# 09-01 Plan: Document Runner Operations and Align Planning Docs

## Objective

<objective>
Document how operators run, debug, and maintain the unified VerifyIQ portal
automation runner without guessing target names, auth prerequisites, artifacts,
cleanup boundaries, or Phase 8 product constraints.

Purpose: satisfy DOCS-04 and DOCS-05 by making README the operator runbook and
updating planning docs to match the final v1.1 runner scope. Output: updated
README operator documentation, aligned planning docs, and verification showing
documentation and runner contracts remain intact. </objective>

## Execution Context

<execution_context>
@/Users/nellvalenzuela/Documents/verifyiq-portalv2/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nellvalenzuela/Documents/verifyiq-portalv2/.codex/get-shit-done/templates/summary.md
</execution_context>

## Context

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/phases/09-runner-documentation-and-regression-operations/09-CONTEXT.md
@.planning/phases/09-runner-documentation-and-regression-operations/09-RESEARCH.md
@.planning/phases/09-runner-documentation-and-regression-operations/09-VALIDATION.md
@.planning/phases/09-runner-documentation-and-regression-operations/09-PATTERNS.md
@.planning/phases/08-deep-portal-workflow-coverage/08-LIVE-INSPECTION.md
@.planning/phases/08-deep-portal-workflow-coverage/08-01-SUMMARY.md
@.planning/phases/08-deep-portal-workflow-coverage/08-02-SUMMARY.md
@.planning/phases/07-automation-owned-mutation-safety-harness/07-01-SUMMARY.md
@AGENTS.md
@README.md
@docs/ai-development-workflow.md
@package.json
@playwright.config.ts
@scripts/run-portal-automation.mjs
@scripts/run-portal-automation.test.mjs
@scripts/summarize-playwright-results.mjs
@scripts/check-docs.mjs
@tests/support/auth-state.ts
@tests/support/automation-records.ts
@tests/support/portal-evidence-workflow.ts
@tests/authenticated/audit-logs-workflow.spec.ts
@tests/authenticated/roles-workflow.spec.ts
</context>

## Decision Coverage

- D-01 through D-04: task 09-01-01 creates the task-first runner usage section,
  examples, and target table.
- D-05 through D-08: task 09-01-02 creates local/CI auth recovery guidance and
  repeats locked precedence and force-login behavior.
- D-09 through D-12: task 09-01-02 creates failure triage order and native
  artifact guidance.
- D-13 through D-16: task 09-01-02 documents same-run cleanup rules and Phase 8
  product constraints.
- DOCS-05: task 09-01-03 updates planning docs after README alignment.

## Threat Model

<threat_model>

| ID      | Threat                                                                               | Severity | Mitigation                                                                                                                |
| ------- | ------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-09-01 | README target names drift from runner source.                                        | high     | Mirror `VALID_TARGETS` and `TARGET_ARGS` from `scripts/run-portal-automation.mjs`; verify exact target strings with `rg`. |
| T-09-02 | Docs imply Audit Logs proves same-run portal activity evidence.                      | high     | State MUT-05 remains a product blocker and only export controls are covered today.                                        |
| T-09-03 | Docs imply Roles edit or reversible permission-toggle coverage exists.               | high     | State MUT-07 role edit/permission-toggle remains blocked until a visible safe edit path exists.                           |
| T-09-04 | Auth recovery docs expose credentials, cookies, tokens, or serialized storage state. | high     | Use variable names and commands only; never include example secret values or storage JSON.                                |
| T-09-05 | Operators treat auth/setup failures as app regressions first.                        | medium   | Document storage-state-first triage and exact recovery commands.                                                          |
| T-09-06 | Docs encourage hidden cleanup APIs or broad deletion.                                | high     | Document visible UI cleanup only and same-run automation-owned identifiers.                                               |
| T-09-07 | Triage summary is described as replacing native Playwright artifacts.                | medium   | Document triage as first read and Playwright artifacts as authoritative.                                                  |
| T-09-08 | Planning docs claim v1.1 coverage beyond product surface.                            | high     | Preserve Phase 8 blocker language in PROJECT/ROADMAP/STATE/REQUIREMENTS.                                                  |
| T-09-09 | Documentation changes break required cross-document references.                      | medium   | Run `npm run docs:check` and `npm run check`.                                                                             |

</threat_model>

## Tasks

<tasks>

<task id="09-01-00" type="auto">
  <name>Confirm execution preconditions and source-of-truth scope</name>
  <files>AGENTS.md, docs/ai-development-workflow.md, README.md, scripts/run-portal-automation.mjs, scripts/summarize-playwright-results.mjs, tests/support/auth-state.ts, tests/support/automation-records.ts, .planning/phases/08-deep-portal-workflow-coverage/08-LIVE-INSPECTION.md</files>
  <read_first>AGENTS.md, docs/ai-development-workflow.md, .planning/phases/09-runner-documentation-and-regression-operations/09-CONTEXT.md, .planning/phases/09-runner-documentation-and-regression-operations/09-RESEARCH.md, .planning/phases/09-runner-documentation-and-regression-operations/09-PATTERNS.md</read_first>
  <action>
    Before implementation starts, run:

    `printf 'Reply exactly: ok\n' | claude --model claude-opus-4-7 -p -`

    If this fails because Claude is missing, logged out, permission blocked, or
    unavailable for setup reasons, stop and report the setup failure. Do not
    treat missing Claude setup as a Codex fallback condition. Claude usage,
    quota, rate-limit, or overload failures may fall back to Codex under
    `docs/ai-development-workflow.md`.

    Confirm source-of-truth strings before editing docs:

    - `VALID_TARGETS` in `scripts/run-portal-automation.mjs`
    - `ARTIFACT_PATHS` in `scripts/run-portal-automation.mjs`
    - `STORAGE_STATE_RECOVERY` in `scripts/summarize-playwright-results.mjs`
    - `AuthStateSource` in `tests/support/auth-state.ts`
    - `AUTOMATION ${context.area} ${context.runId}` semantics in
      `tests/support/automation-records.ts`
    - `MUT-05 Audit Logs evidence blocker` and `MUT-07 role edit blocker` in
      `08-LIVE-INSPECTION.md`

    Do not edit runtime files in this task.

  </action>
  <verify>
    `printf 'Reply exactly: ok\n' | claude --model claude-opus-4-7 -p -`
    `rg -n 'VALID_TARGETS|ARTIFACT_PATHS|PORTAL_TAGS' scripts/run-portal-automation.mjs`
    `rg -n 'STORAGE_STATE_RECOVERY|test-results/triage-summary.md|playwright-report/' scripts/summarize-playwright-results.mjs scripts/run-portal-automation.mjs`
    `rg -n 'VERIFYIQ_STORAGE_STATE_JSON|VERIFYIQ_STORAGE_STATE_PATH|playwright/.auth/user.json|credential-login' tests/support/auth-state.ts`
    `rg -n 'AUTOMATION|runId|registerAutomationRecord|assertSameRunMutationTarget' tests/support/automation-records.ts`
    `rg -n 'MUT-05 Audit Logs evidence blocker|MUT-07 role edit blocker' .planning/phases/08-deep-portal-workflow-coverage/08-LIVE-INSPECTION.md`
  </verify>
  <acceptance_criteria>
    - Claude preflight exits 0 and outputs `ok`, or a documented allowed fallback condition exists.
    - `scripts/run-portal-automation.mjs` contains `VALID_TARGETS`.
    - `scripts/run-portal-automation.mjs` contains `ARTIFACT_PATHS`.
    - `scripts/summarize-playwright-results.mjs` contains `STORAGE_STATE_RECOVERY`.
    - `tests/support/auth-state.ts` contains `VERIFYIQ_STORAGE_STATE_JSON`.
    - `tests/support/automation-records.ts` contains `assertSameRunMutationTarget`.
    - `08-LIVE-INSPECTION.md` contains `MUT-05 Audit Logs evidence blocker`.
    - `08-LIVE-INSPECTION.md` contains `MUT-07 role edit blocker`.
  </acceptance_criteria>
  <done>Execution starts from current runner behavior and Phase 8 product constraints.</done>
</task>

<task id="09-01-01" type="auto">
  <name>Add task-first runner operations README section</name>
  <files>README.md</files>
  <read_first>README.md, package.json, scripts/run-portal-automation.mjs, scripts/run-portal-automation.test.mjs, .planning/phases/09-runner-documentation-and-regression-operations/09-CONTEXT.md, .planning/phases/09-runner-documentation-and-regression-operations/09-PATTERNS.md</read_first>
  <action>
    Update `README.md` with a reachable section named exactly:

    `## Portal Runner Operations`

    Place it before or near `## Commands` so operators encounter runner usage
    before the general command reference.

    Add a `### Quick Start` subsection with these exact command examples:

    - `npm run test:portal`
    - `npm run test:portal -- applications`
    - `npm run test:portal -- activity -- --headed`
    - `npm run test:portal -- users -- --trace on`

    State that native Playwright flags go after the second `--`.

    Add a compact target table with exactly these targets and commands:

    - `all` -> `npm run test:portal`
    - `public` -> `npm run test:portal -- public`
    - `auth` -> `npm run test:portal -- auth`
    - `applications` -> `npm run test:portal -- applications`
    - `activity` -> `npm run test:portal -- activity`
    - `audit-logs` -> `npm run test:portal -- audit-logs`
    - `users` -> `npm run test:portal -- users`
    - `roles` -> `npm run test:portal -- roles`

    The table must include an auth requirement column. Mark only `public` as
    not requiring storage state; all other targets that include authenticated
    projects require valid auth state.

    The `audit-logs` row must state that export controls are covered and
    same-run portal activity evidence remains a `MUT-05` product blocker. The
    `roles` row must state that create/delete coverage exists and role edit
    plus reversible permission-toggle coverage remains a `MUT-07` product
    blocker.

    Add an artifact list that names:

    - `test-results/triage-summary.md`
    - `playwright-report/`
    - `test-results/results.json`
    - `test-results/artifacts/`

    Keep the existing `## Commands` list, but update its `npm run test:portal`
    bullet if needed so it points readers to `Portal Runner Operations` and
    does not duplicate stale target details.

  </action>
  <verify>
    `rg -n '## Portal Runner Operations|### Quick Start|npm run test:portal -- activity -- --headed|npm run test:portal -- users -- --trace on|Native Playwright flags' README.md`
    `rg -n 'audit-logs|MUT-05|roles|MUT-07|test-results/triage-summary.md|playwright-report/|test-results/results.json|test-results/artifacts/' README.md`
    `npm run docs:check`
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Portal Runner Operations`.
    - `README.md` contains `### Quick Start`.
    - `README.md` contains `npm run test:portal`.
    - `README.md` contains `npm run test:portal -- public`.
    - `README.md` contains `npm run test:portal -- audit-logs`.
    - `README.md` contains `MUT-05`.
    - `README.md` contains `MUT-07`.
    - `README.md` contains `test-results/triage-summary.md`.
    - `README.md` contains `playwright-report/`.
    - `npm run docs:check` exits 0.
  </acceptance_criteria>
  <done>README contains the operator-facing runner quick start, target table, product constraints, and artifact list.</done>
</task>

<task id="09-01-02" type="auto">
  <name>Document auth recovery, triage order, and same-run cleanup rules</name>
  <files>README.md</files>
  <read_first>README.md, scripts/summarize-playwright-results.mjs, tests/support/auth-state.ts, tests/support/automation-records.ts, .planning/phases/08-deep-portal-workflow-coverage/08-LIVE-INSPECTION.md, .planning/phases/08-deep-portal-workflow-coverage/08-01-SUMMARY.md, .planning/phases/08-deep-portal-workflow-coverage/08-02-SUMMARY.md</read_first>
  <action>
    Extend the `README.md` runner documentation with three subsections. They
    may live under `## Portal Runner Operations` or the existing
    `## Regression Maintenance` section, but must be reachable from the runner
    section.

    Add or update a subsection named:

    `### Runner Auth Recovery`

    It must state this exact precedence in order:

    `VERIFYIQ_STORAGE_STATE_JSON`, `VERIFYIQ_STORAGE_STATE_PATH`,
    `playwright/.auth/user.json`, credential login.

    It must state exactly that `VERIFYIQ_FORCE_LOGIN=1` bypasses only local
    `playwright/.auth/user.json` and does not bypass
    `VERIFYIQ_STORAGE_STATE_JSON` or `VERIFYIQ_STORAGE_STATE_PATH`.

    It must include recovery guidance for these cases:

    - no local auth state -> run `npm run auth:record`
    - expired local auth state -> run `npm run auth:record`
    - malformed `VERIFYIQ_STORAGE_STATE_JSON` -> refresh the secret with a
      valid Playwright storage-state JSON object, without printing it
    - CI auth setup failure -> refresh `VERIFYIQ_STORAGE_STATE_JSON` or
      `VERIFYIQ_STORAGE_STATE_PATH`
    - force-login debugging -> use `VERIFYIQ_FORCE_LOGIN=1` only to bypass the
      ignored local file

    Add or update a subsection named:

    `### Failure Debug Order`

    It must prescribe this order:

    1. `test-results/triage-summary.md`
    2. `playwright-report/`
    3. `test-results/results.json`
    4. `test-results/artifacts/`

    It must state that the triage summary is a lean operator guide and native
    Playwright artifacts remain authoritative.

    Add or update a subsection named:

    `### Same-Run Cleanup Rules`

    It must state that mutating workflow tests may update, delete, deactivate,
    or clean up only records created and registered in the same automation run.
    It must include the exact identifier shape:

    `AUTOMATION <area> <run-id> <record-label>`

    It must say cleanup uses visible UI controls only, hidden cleanup APIs are
    out of scope, and cleanup residue should be handled through secret-safe
    diagnostics and visible automation identifiers.

    Do not include credential values, cookies, tokens, serialized storage
    state, `.env` values, broad DOM dumps, or screenshots in README examples.

  </action>
  <verify>
    `rg -n '### Runner Auth Recovery|VERIFYIQ_STORAGE_STATE_JSON.*VERIFYIQ_STORAGE_STATE_PATH.*playwright/.auth/user.json.*credential login|VERIFYIQ_FORCE_LOGIN=1|npm run auth:record' README.md`
    `rg -n '### Failure Debug Order|test-results/triage-summary.md|playwright-report/|test-results/results.json|test-results/artifacts/|native Playwright artifacts remain' README.md`
    `rg -n '### Same-Run Cleanup Rules|AUTOMATION <area> <run-id> <record-label>|same automation run|visible UI controls only|hidden cleanup APIs' README.md`
    `npm run docs:check`
  </verify>
  <acceptance_criteria>
    - `README.md` contains `### Runner Auth Recovery`.
    - `README.md` contains `VERIFYIQ_STORAGE_STATE_JSON`.
    - `README.md` contains `VERIFYIQ_FORCE_LOGIN=1`.
    - `README.md` contains `### Failure Debug Order`.
    - `README.md` contains `native Playwright artifacts remain`.
    - `README.md` contains `### Same-Run Cleanup Rules`.
    - `README.md` contains `AUTOMATION <area> <run-id> <record-label>`.
    - `README.md` contains `hidden cleanup APIs`.
    - `npm run docs:check` exits 0.
  </acceptance_criteria>
  <done>README documents auth recovery, failure triage order, and safe cleanup rules without exposing secrets.</done>
</task>

<task id="09-01-03" type="auto">
  <name>Align planning docs with final v1.1 runner operations</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, README.md, .planning/phases/09-runner-documentation-and-regression-operations/09-CONTEXT.md, .planning/phases/09-runner-documentation-and-regression-operations/09-RESEARCH.md, .planning/phases/08-deep-portal-workflow-coverage/08-LIVE-INSPECTION.md</read_first>
  <action>
    Update planning docs after README runner documentation is in place.

    In `.planning/PROJECT.md`:

    - Add a validated Phase 9 bullet stating that README documents runner
      targets, auth prerequisites, recovery commands, artifacts, Phase 8 product
      constraints, and same-run cleanup rules.
    - Keep Playwright tests as the source of truth.
    - Keep the Phase 8 blocker decision language for Audit Logs same-run portal
      activity evidence and Roles edit coverage.
    - Update the current status/date only if the phase execution is complete.

    In `.planning/REQUIREMENTS.md`:

    - Mark `DOCS-04` as complete only after README contains the runner setup,
      targets, auth prerequisites, examples, artifacts, and recovery commands.
    - Mark `DOCS-05` as complete only after PROJECT, ROADMAP, STATE, and
      REQUIREMENTS reflect v1.1 runner scope, Playwright source-of-truth rules,
      and safe workflow boundaries.
    - Keep the Phase 8 MUT-05/MUT-07 blocker note.

    In `.planning/ROADMAP.md`:

    - Mark Phase 9 and plan `09-01` complete only after `npm run check` passes.
    - Keep Phase 9 Success Criteria intact.
    - Keep v1.1 coverage totals at 24 mapped requirements.

    In `.planning/STATE.md`:

    - Update current focus, plan status, totals, session timestamp, and next
      operator step to match completed Phase 9 documentation.
    - If final milestone completion is the next step, name
      `$gsd-complete-milestone` as the next GSD command.

    Do not claim new runner behavior, new auth precedence, hidden cleanup, or
    product coverage that Phase 8 left blocked.

  </action>
  <verify>
    `rg -n 'Phase 9|runner documentation|DOCS-04|DOCS-05|Playwright source of truth|automation-owned|MUT-05|MUT-07' .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md`
    `npm run docs:check`
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `Phase 9`.
    - `.planning/PROJECT.md` contains `same-run cleanup`.
    - `.planning/REQUIREMENTS.md` contains `DOCS-04`.
    - `.planning/REQUIREMENTS.md` contains `DOCS-05`.
    - `.planning/ROADMAP.md` contains `09-01`.
    - `.planning/STATE.md` contains `Phase 09`.
    - Planning docs preserve `MUT-05` and `MUT-07` blocker language.
    - `npm run docs:check` exits 0.
  </acceptance_criteria>
  <done>Planning docs match the final v1.1 runner documentation and safe workflow boundaries.</done>
</task>

<task id="09-01-04" type="auto">
  <name>Run final documentation and runner verification</name>
  <files>README.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, scripts/run-portal-automation.mjs, scripts/run-portal-automation.test.mjs</files>
  <read_first>README.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, package.json, scripts/run-portal-automation.mjs, scripts/run-portal-automation.test.mjs, AGENTS.md</read_first>
  <action>
    Run final verification:

    1. `npm run docs:check`
    2. `npm run test:portal:unit`
    3. `npm run check`
    4. `npm run test:portal -- public`

    Confirm runtime files were not modified unless a defect required it:

    `git diff -- scripts/run-portal-automation.mjs scripts/run-portal-automation.test.mjs scripts/summarize-playwright-results.mjs tests/support/auth-state.ts tests/support/automation-records.ts`

    If valid auth state is available, run relevant authenticated runner
    commands for final milestone confidence:

    - `npm run test:portal -- applications`
    - `npm run test:portal -- activity`
    - `npm run test:portal -- audit-logs`
    - `npm run test:portal -- users`
    - `npm run test:portal -- roles`

    If auth state is missing or expired, do not weaken docs or tests. Record the
    auth-gated verification blocker and recovery command `npm run auth:record`
    in the summary without printing secrets.

  </action>
  <verify>
    `npm run docs:check`
    `npm run test:portal:unit`
    `npm run check`
    `npm run test:portal -- public`
    `git diff -- scripts/run-portal-automation.mjs scripts/run-portal-automation.test.mjs scripts/summarize-playwright-results.mjs tests/support/auth-state.ts tests/support/automation-records.ts`
  </verify>
  <acceptance_criteria>
    - `npm run docs:check` exits 0.
    - `npm run test:portal:unit` exits 0.
    - `npm run check` exits 0.
    - `npm run test:portal -- public` exits 0 or failure is diagnosed as an existing public smoke issue without changing Phase 9 docs truth.
    - Runtime source diff for runner, triage, auth, and mutation helper files is empty unless explicitly justified in the summary.
    - If authenticated target commands are skipped, the summary names valid storage state and `npm run auth:record` as the recovery path.
  </acceptance_criteria>
  <done>Phase 9 docs and runner contract are verified, with auth-gated checks recorded honestly.</done>
</task>

</tasks>

## Verification

<verification>

Run these before marking Phase 9 complete:

1. `npm run docs:check`
2. `npm run test:portal:unit`
3. `npm run check`
4. `npm run test:portal -- public`

When valid auth state is available, also run:

1. `npm run test:portal -- applications`
2. `npm run test:portal -- activity`
3. `npm run test:portal -- audit-logs`
4. `npm run test:portal -- users`
5. `npm run test:portal -- roles`

If authenticated verification is blocked by missing or expired storage state,
record the blocker and the recovery command `npm run auth:record`. Do not print
credentials, cookies, tokens, serialized storage state, or `.env` values.

</verification>

## Success Criteria

<success_criteria>

- README contains `## Portal Runner Operations` with quick-start snippets,
  target table, auth requirements, artifact paths, and product constraints.
- README contains storage-state-first auth recovery with exact precedence and
  `VERIFYIQ_FORCE_LOGIN=1` behavior.
- README contains failure debug order with triage summary first and native
  Playwright artifacts authoritative.
- README contains same-run cleanup rules, visible UI cleanup only, and
  `AUTOMATION <area> <run-id> <record-label>`.
- `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and
  `.planning/REQUIREMENTS.md` reflect the final v1.1 runner documentation scope
  and preserve MUT-05/MUT-07 blocker language.
- `npm run docs:check`, `npm run test:portal:unit`, and `npm run check` pass.
- Authenticated runner commands are run when valid storage state is available,
  or skipped with the documented `npm run auth:record` recovery path.

</success_criteria>

## Notes For Executor

- Keep edits documentation-focused. Runtime code should not change unless a doc
  verification command exposes an existing defect.
- Preserve public-repo secret safety. Do not paste `.env`, credential values,
  cookies, tokens, serialized storage state, or raw browser storage into docs or
  summaries.
- Browser helpers may be used for exploration only. Final coverage remains
  committed Playwright tests and commands.
