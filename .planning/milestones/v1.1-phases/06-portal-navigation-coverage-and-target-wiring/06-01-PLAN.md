---
phase: 06-portal-navigation-coverage-and-target-wiring
plan: 01
plan_id: 06-01
status: planned
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run-portal-automation.mjs
  - scripts/run-portal-automation.test.mjs
  - tests/authenticated/add-application.spec.ts
  - README.md
autonomous: true
requirements: [RUN-02]
user_setup: []
must_haves:
  truths:
    - "D-02/RUN-02: Portal-specific runner targets use Playwright-native grep
      tags, not browser logic in the runner."
    - "D-03: The `applications` target selects `@portal:applications`, which
      includes existing Add Application coverage after test titles are tagged."
    - "D-04: `auth` remains all authenticated specs and `all` remains the bare
      Playwright test invocation across projects."
    - "D-12: Documentation changes stay minimal: README lists the runner
      command, target names, and passthrough syntax only."
  artifacts:
    - path: "scripts/run-portal-automation.mjs"
      provides: "Portal target grep mappings"
      contains: "@portal:activity"
    - path: "scripts/run-portal-automation.test.mjs"
      provides: "Unit coverage for every target mapping"
      contains: "@portal:audit-logs"
    - path: "tests/authenticated/add-application.spec.ts"
      provides: "Applications target tag on existing deep coverage"
      contains: "@portal:applications"
    - path: "README.md"
      provides: "Minimal runner target reference"
      contains: "npm run test:portal"
  key_links:
    - from: "scripts/run-portal-automation.mjs"
      to: "tests/authenticated/add-application.spec.ts"
      via: "Playwright --grep tag"
      pattern: "@portal:applications"
    - from: "scripts/run-portal-automation.mjs"
      to: "playwright.config.ts"
      via: "authenticated project selection"
      pattern: "--project=authenticated-chromium"
---

# 06-01 Plan: Add Portal Target Wiring and Applications Tagging

## Objective

<objective>
Replace Phase 5 placeholder portal target mappings with stable
Playwright-native grep mappings for `applications`, `activity`, `audit-logs`,
`users`, and `roles`, and tag existing Add Application tests so the
`applications` target still includes the established Applications workflow
coverage.

Purpose: make every required runner target predictable before adding the new
portal navigation spec. Output: updated runner mapping helpers, runner unit
tests, tagged Add Application tests, and a minimal README target reference.
</objective>

## Execution Context

<execution_context>
@/Users/nellvalenzuela/Documents/verifyiq-portalv2/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nellvalenzuela/Documents/verifyiq-portalv2/.codex/get-shit-done/templates/summary.md
</execution_context>

## Context

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-portal-navigation-coverage-and-target-wiring/06-CONTEXT.md
@.planning/phases/06-portal-navigation-coverage-and-target-wiring/06-RESEARCH.md
@.planning/phases/06-portal-navigation-coverage-and-target-wiring/06-VALIDATION.md
@.planning/phases/06-portal-navigation-coverage-and-target-wiring/06-PATTERNS.md
@AGENTS.md
@README.md
@docs/ai-development-workflow.md
@package.json
@playwright.config.ts
@scripts/run-portal-automation.mjs
@scripts/run-portal-automation.test.mjs
@tests/authenticated/add-application.spec.ts
</context>

## Decision Coverage

- D-02: use stable Playwright grep tags for in-spec target selection.
- D-03: tag Add Application tests with `@portal:applications` so the
  Applications target keeps existing deep coverage.
- D-04: keep `auth` and `all` target semantics unchanged.
- D-12: update only minimal command/target references in README.

## Threat Model

<threat_model>

| ID      | Threat                                                                               | Severity | Mitigation                                                                                              |
| ------- | ------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------- |
| T-06-01 | Runner target filtering drops existing Add Application coverage from `applications`. | high     | Tag every existing Add Application test with `@portal:applications` and map `applications` to that tag. |
| T-06-02 | Runner grows browser automation or route assumptions.                                | high     | Only change Playwright CLI args; do not import `@playwright/test` or add browser code to the runner.    |
| T-06-03 | `auth` or `all` becomes narrower than the project contract.                          | high     | Leave `auth` as `["test", "--project=authenticated-chromium"]` and `all` as `["test"]`.                 |
| T-06-04 | README expands into Phase 9 runbook scope early.                                     | low      | Add only command, target list, and passthrough examples.                                                |
| T-06-05 | Runner output exposes credentials or storage state.                                  | high     | Do not print env vars, `.env`, `playwright/.auth/`, cookies, tokens, or storage-state JSON.             |

</threat_model>

## Tasks

<tasks>

<task id="06-01-00" type="auto">
  <name>Verify Claude Opus execution preflight</name>
  <files>AGENTS.md, docs/ai-development-workflow.md</files>
  <read_first>AGENTS.md, docs/ai-development-workflow.md</read_first>
  <action>
    Before implementation starts, run:

    `printf 'Reply exactly: ok\n' | claude --model claude-opus-4-7 -p -`

    If this fails because Claude is missing, logged out, permission blocked, or
    unavailable for setup reasons, stop and report the setup failure. Do not
    treat missing Claude setup as a Codex fallback condition.

  </action>
  <verify>
    `printf 'Reply exactly: ok\n' | claude --model claude-opus-4-7 -p -` exits 0 and outputs `ok`.
  </verify>
  <acceptance_criteria>
    - Command output contains `ok`.
    - Command exits 0.
  </acceptance_criteria>
  <done>Claude first-pass implementation preflight succeeded or setup failure was reported without modifying source files.</done>
</task>

<task id="06-01-01" type="auto">
  <name>Map portal runner targets to Playwright grep tags</name>
  <files>scripts/run-portal-automation.mjs, scripts/run-portal-automation.test.mjs</files>
  <read_first>scripts/run-portal-automation.mjs, scripts/run-portal-automation.test.mjs, playwright.config.ts, .planning/phases/06-portal-navigation-coverage-and-target-wiring/06-PATTERNS.md</read_first>
  <action>
    Update `scripts/run-portal-automation.mjs` without adding dependencies and
    without importing `@playwright/test`.

    Add or inline these exact portal tag values:

    - `applications`: `@portal:applications`
    - `activity`: `@portal:activity`
    - `audit-logs`: `@portal:audit-logs`
    - `users`: `@portal:users`
    - `roles`: `@portal:roles`

    Update target mappings so `buildPlaywrightArgs(target)` returns exactly:

    - `all`: `["test"]`
    - `public`: `["test", "--project=public-smoke"]`
    - `auth`: `["test", "--project=authenticated-chromium"]`
    - `applications`: `["test", "--project=authenticated-chromium", "--grep", "@portal:applications"]`
    - `activity`: `["test", "--project=authenticated-chromium", "--grep", "@portal:activity"]`
    - `audit-logs`: `["test", "--project=authenticated-chromium", "--grep", "@portal:audit-logs"]`
    - `users`: `["test", "--project=authenticated-chromium", "--grep", "@portal:users"]`
    - `roles`: `["test", "--project=authenticated-chromium", "--grep", "@portal:roles"]`

    Keep passthrough behavior unchanged: extra args after the explicit `--`
    separator append after these target mappings.

    Update `scripts/run-portal-automation.test.mjs` so exact-array assertions
    cover every mapping above. Replace the old placeholder test named
    `buildPlaywrightArgs maps deep targets to authenticated coverage for now`
    with assertions that each portal target includes its expected `--grep`
    tag.

  </action>
  <verify>
    `rg -n '@portal:applications' scripts/run-portal-automation.mjs scripts/run-portal-automation.test.mjs`
    `rg -n '@portal:activity' scripts/run-portal-automation.mjs scripts/run-portal-automation.test.mjs`
    `rg -n '@portal:audit-logs' scripts/run-portal-automation.mjs scripts/run-portal-automation.test.mjs`
    `rg -n '@portal:users' scripts/run-portal-automation.mjs scripts/run-portal-automation.test.mjs`
    `rg -n '@portal:roles' scripts/run-portal-automation.mjs scripts/run-portal-automation.test.mjs`
    `npm run test:portal:unit`
  </verify>
  <acceptance_criteria>
    - `buildPlaywrightArgs("applications")` is unit-tested as `["test", "--project=authenticated-chromium", "--grep", "@portal:applications"]`.
    - `buildPlaywrightArgs("activity")` is unit-tested with `@portal:activity`.
    - `buildPlaywrightArgs("audit-logs")` is unit-tested with `@portal:audit-logs`.
    - `buildPlaywrightArgs("users")` is unit-tested with `@portal:users`.
    - `buildPlaywrightArgs("roles")` is unit-tested with `@portal:roles`.
    - `buildPlaywrightArgs("auth")` remains `["test", "--project=authenticated-chromium"]`.
    - `buildPlaywrightArgs("all")` remains `["test"]`.
    - `npm run test:portal:unit` exits 0.
  </acceptance_criteria>
  <done>Runner target mappings are predictable and locked by unit tests.</done>
</task>

<task id="06-01-02" type="auto">
  <name>Tag existing Add Application coverage for the Applications target</name>
  <files>tests/authenticated/add-application.spec.ts</files>
  <read_first>tests/authenticated/add-application.spec.ts, tests/support/application-workflow.ts, tests/support/page-errors.ts, .planning/phases/06-portal-navigation-coverage-and-target-wiring/06-CONTEXT.md</read_first>
  <action>
    In `tests/authenticated/add-application.spec.ts`, add the exact tag
    `@portal:applications` to every test title without changing test behavior.

    Add a local constant:

    ```typescript
    const applicationsTargetTag = "@portal:applications";
    ```

    For the document-type matrix, keep the existing generated human-readable
    test names and append the tag:

    ```typescript
    test(`${testName} ${applicationsTargetTag}`, async ({ page }, testInfo) => {
    ```

    For the validation test, update the title to:

    ```typescript
    test(`authenticated user sees required applicant validation ${applicationsTargetTag}`, async ({ page }, testInfo) => {
    ```

    Do not change the Add Application workflow, upload fixture, inline
    `validation-error` assertion, page-error collection, URL assertions, or
    `attachFormInventory` catch block.

  </action>
  <verify>
    `rg -n '@portal:applications' tests/authenticated/add-application.spec.ts`
    `rg -n 'validation-error' tests/authenticated/add-application.spec.ts`
    `rg -n 'attachFormInventory' tests/authenticated/add-application.spec.ts`
    `npm run test:portal:unit`
  </verify>
  <acceptance_criteria>
    - `tests/authenticated/add-application.spec.ts` contains `const applicationsTargetTag = "@portal:applications"`.
    - The document-type matrix uses `${testName} ${applicationsTargetTag}`.
    - The required applicant validation test title includes `${applicationsTargetTag}`.
    - `tests/authenticated/add-application.spec.ts` still contains `getByTestId("validation-error")`.
    - `tests/authenticated/add-application.spec.ts` still contains `attachFormInventory`.
  </acceptance_criteria>
  <done>Existing Applications deep coverage is selectable by the Applications runner target.</done>
</task>

<task id="06-01-03" type="auto">
  <name>Add minimal README runner target reference</name>
  <files>README.md</files>
  <read_first>README.md, package.json, scripts/run-portal-automation.mjs, .planning/phases/06-portal-navigation-coverage-and-target-wiring/06-CONTEXT.md</read_first>
  <action>
    Update README's Commands section with a concise `npm run test:portal`
    entry. Keep this scoped to Phase 6 behavior; do not write the full Phase 9
    runner runbook.

    Add wording that includes:

    - `npm run test:portal` defaults to `all`.
    - Target syntax is `npm run test:portal -- <target>`.
    - Valid targets are `all`, `public`, `auth`, `applications`, `activity`,
      `audit-logs`, `users`, and `roles`.
    - Native Playwright flags go after a second separator, for example
      `npm run test:portal -- activity -- --headed`.

    Do not document credentials, cookies, tokens, or storage-state content.

  </action>
  <verify>
    `rg -n 'npm run test:portal' README.md`
    `rg -n 'activity' README.md`
    `rg -n 'audit-logs' README.md`
    `npm run docs:check`
  </verify>
  <acceptance_criteria>
    - README contains `npm run test:portal`.
    - README contains all eight target names.
    - README contains `npm run test:portal -- activity -- --headed`.
    - `npm run docs:check` exits 0.
  </acceptance_criteria>
  <done>Minimal target reference is available from the human entrypoint.</done>
</task>

</tasks>

## Verification

<verification>

Run after this plan:

1. `npm run test:portal:unit`
2. `npm run docs:check`
3. `npm run check`

</verification>

## Success Criteria

<success_criteria>

- Runner target allowlist still contains exactly `all`, `public`, `auth`,
  `applications`, `activity`, `audit-logs`, `users`, and `roles`.
- Portal-specific targets map to exact `@portal:*` grep tags.
- `auth` and `all` mappings are unchanged from Phase 5.
- Existing Add Application tests are tagged with `@portal:applications`.
- README has a minimal target reference without replacing the Phase 9 runbook.
- `npm run check` passes after runner and docs changes.

</success_criteria>

## Output

<output>
After completion, create `.planning/phases/06-portal-navigation-coverage-and-target-wiring/06-01-SUMMARY.md`.
</output>
